version: 2.1

orbs:
  python: circleci/python@1.2
  sonarcloud: sonarsource/sonarcloud@1.0.2

workflows:
  sample:
    jobs:
      - build:
          context: SonarCloud

jobs:
  build:
    docker:
      - image: cimg/python:3.8

    steps:
      - checkout
      - python/install-packages:
          pkg-manager: pip
      # Scan our project in sonarcloud.
      - sonarcloud/scan
      - run:
          name: Echo Done
          command: echo "Done."
